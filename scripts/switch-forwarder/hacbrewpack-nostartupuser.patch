diff --git a/main.c b/main.c
--- a/main.c
+++ b/main.c
@@ -45,7 +45,8 @@ static void usage(void)
             "--titleid                Use specified titleid for creating ncas and patch titleid in npdm and nacp\n"
             "--titlename              Change title name in nacp for all languages, max size is 512 bytes\n"
             "--titlepublisher         Change title publisher in nacp for all languages, max size is 256 bytes\n"
-            "--nopatchnacplogo        Skip changing logo handeling to auto in NACP\n",
+            "--nopatchnacplogo        Skip changing logo handeling to auto in NACP\n"
+            "--nostartupuser          Set StartupUserAccount to None (no user selection on launch)\n",
             USAGE_PROGRAM_NAME);
     exit(EXIT_FAILURE);
 }
@@ -131,6 +132,7 @@ int main(int argc, char **argv)
                 {"nosignncasig2", 0, NULL, 20},
                 {"legalinfodir", 1, NULL, 21},
                 {"backupdir", 1, NULL, 22},
+                {"nostartupuser", 0, NULL, 23},
                 {NULL, 0, NULL, 0},
             };
 
@@ -241,6 +243,9 @@ int main(int argc, char **argv)
         case 22:
             filepath_set(&settings.backup_dir, optarg);
             break;
+        case 23:
+            settings.nostartupuser = 1;
+            break;
         default:
             usage();
         }
diff --git a/nacp.c b/nacp.c
--- a/nacp.c
+++ b/nacp.c
@@ -88,6 +88,14 @@ void nacp_process(hbp_settings_t *settings)
         nacp.LogoHandling = 0x00;
     }
 
+    // Set StartupUserAccount to None (no user selection prompt on launch)
+    if (settings->nostartupuser == 1)
+    {
+        printf("Setting StartupUserAccount to None (no user selection)\n");
+        nacp.StartupUserAccount = 0x00;
+    }
+
     if (settings->title_id != 0)
     {
         printf("Setting TitleIDs\n");
@@ -98,7 +106,7 @@ void nacp_process(hbp_settings_t *settings)
     }
 
     // Backup and re-write NACP
-    if (settings->titlename[0] != 0x00 || settings->titlepublisher[0] != 0x00 || settings->title_id != 0 || settings->nopatchnacplogo == 0)
+    if (settings->titlename[0] != 0x00 || settings->titlepublisher[0] != 0x00 || settings->title_id != 0 || settings->nopatchnacplogo == 0 || settings->nostartupuser == 1)
     {
         // Copy control.nacp to backup directory
         struct timeval ct;
diff --git a/settings.h b/settings.h
--- a/settings.h
+++ b/settings.h
@@ -70,6 +70,7 @@ typedef struct
     uint8_t keepncadir;
     uint8_t nopatchnacplogo;
     uint8_t nosignncasig2;
+    uint8_t nostartupuser;
     uint64_t title_id;
     char titlename[0x200];
     char titlepublisher[0x200];
